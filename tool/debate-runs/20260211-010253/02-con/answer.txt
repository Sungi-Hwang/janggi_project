`2026-02-11 01:03 (GMT+9)` 기준으로 안정화 전략은 아래 6개로 정리됩니다.

1. 타임아웃을 단계별로 분리  
`pro/con`, `retry`, `synthesis`를 같은 제한으로 두지 말고 각각 SLA를 설정.

2. 재시도 정책 고도화  
고정 지연 대신 `exponential backoff + jitter`, 최대 재시도 횟수 제한, 실패 유형별 재시도 여부 분리.

3. 부분 성공 허용  
한쪽 에이전트만 성공해도 축약 합성 경로로 진행해 `blocked` 종료를 줄임.

4. 병렬 실행은 유지, 프로세스 스폰 비용은 축소  
`Start-Job` 남발 대신 Runspace Pool/Thread 기반으로 전환해 tail latency 완화.

5. 파싱 강건성 강화  
JSON 실패 시 즉시 `plain_text` fallback으로 수용하고 후처리에서 정규화.

6. 운영 지표 고정  
`p50/p95 latency`, `timeout rate`, `retry success rate`, `blocked rate`를 런마다 저장해 튜닝 전후 비교.

원하면 다음 단계로 `tool/ai_debate.ps1` 기준 최소 변경 안정화 패치안을 바로 줄게요.
